function remainingMethods(
  n: number,
  k: number,
  invocations: number[][]
): number[] {
  let m = new Array(n).fill(-1)
  m[k] = 0
  for(let i = 0;i<n;++i){
    for(let [a,b] of invocations){

    }
  }
}
remainingMethods(4, 1, [
  [1, 2],
  [0, 1],
  [3, 2],
])
